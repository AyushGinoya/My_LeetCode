class Solution {
public:
    int buyChoco(vector<int>& p, int m) {
        sort(p.begin(),p.end());
        int s = m;
        
        m = m-p[0];
        m = m-p[1];
  
        if(m<0)
            return s; 
        
        return m;

    }
};
